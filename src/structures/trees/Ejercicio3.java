package structures.trees;
import java.util.ArrayDeque;
import java.util.LinkedList;
import java.util.List;
import structures.node.Node;
public class Ejercicio3 {
    //public List<List<Node>> listLevels(Node root){ //lista de lista de nodos

        //se puede instanciar una lista de lista (puede ser de tipo linkedlist)
        //List<List<Node>> listaNiveles = new LinkedList<>();
        //if(root == null){
           // return listaNiveles;
        //}
        //una cola va sacando elementos en el orden que son = entonces tengo ya ordenados
        //ArrayDeque<Node> colaNodos = new ArrayDeque<>();
        //colaNodos.add(root);
        //while(!colaNodos.isEmpty()){
            //int tamanio = colaNodos.size();
            //List<Node> nivelesNodo = new LinkedList<>();
            //colaNodos.poll();
            //for(int i = 0; i < tamanio; i++){
                //Node actual = colaNodos.poll();
                //nivelesNodo.add(actual); //los que siguen igual
                //if(actual.getLeft() != null){
                    //colaNodos.add(actual.getLeft());
                //}
                //if(actual.getRight() != null){
                    //colaNodos.add(actual.getRight());
                //}
            //}
            //listaNiveles.add(nivelesNodo);   
        }
        //return listaNiveles;
    //}
//}
